---
import { Image } from 'astro:assets';

const testimonials = [
  {
    quote: "Integrating Payflo's payroll API into our platform took just weeks, not months. Our customers love having payroll built right into our software.",
    author: "Sarah Chen",
    role: "CTO at WorkFlow",
    avatar: "/testimonial-avatar-1.jpg"
  },
  {
    quote: "The Payflo team guided us through every step of the implementation process. Their support has been exceptional from day one.",
    author: "Marcus Johnson",
    role: "Product Lead at StaffHub",
    avatar: "/testimonial-avatar-2.jpg"
  },
  {
    quote: "Being able to offer embedded payroll has been a game-changer for customer retention. Payflo's infrastructure makes it seem like we built it ourselves.",
    author: "Priya Patel",
    role: "CEO at TeamSync",
    avatar: "/testimonial-avatar-3.jpg"
  }
];
---

<section class="py-20 bg-white">
  <div class="container-custom">
    <div class="text-center max-w-3xl mx-auto animate-on-scroll">
      <h2 class="text-3xl md:text-4xl font-bold">
        Trusted by innovative SaaS platforms
      </h2>
      <p class="mt-4 text-xl text-gray-600">
        See what our customers have to say about building with Payflo.
      </p>
    </div>
    
    <div class="mt-16 grid grid-cols-1 md:grid-cols-3 gap-8">
      {testimonials.map((testimonial, index) => (
        <div 
          class="bg-payflo-gray rounded-xl p-8 animate-on-scroll" 
          style={`animation-delay: ${index * 0.15}s`}
        >
          <div class="flex items-center mb-6">
            <img 
              src={testimonial.avatar} 
              alt={testimonial.author} 
              class="w-12 h-12 rounded-full mr-4"
            />
            <div>
              <h4 class="font-semibold">{testimonial.author}</h4>
              <p class="text-sm text-gray-600">{testimonial.role}</p>
            </div>
          </div>
          <p class="text-gray-700 italic">"{testimonial.quote}"</p>
        </div>
      ))}
    </div>
  </div>
</section>
